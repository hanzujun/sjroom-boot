spring:
  cloud:
    consul:
      config:
        data-key: data
        defaultContext: application
        enabled: true
        format: yaml
        prefix: config
        profileSeparator: ','
        watch:
          enabled: false
